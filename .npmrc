; npm supply chain defaults (npm-guard)
; postinstall 経由の攻撃を遮断（必要な場合は npm rebuild <pkg> で個別許可）
ignore-scripts=true
; install/audit で high 以上の脆弱性を警告
audit-level=high
; ^ や ~ を付けず完全固定
save-exact=true
; lockfile を必ず生成
package-lock=true
